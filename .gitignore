# Dependencies and build output
node_modules/
.next/
dist/
build/
coverage/
*.tsbuildinfo

# Local env and secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials*
secrets*
all_keys.json

# Runtime databases and private chat data
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
radar.db
collector.db
assistant.db
decrypted/
.wechat-radar/
.hive/

# Logs and OS files
*.log
logs/
.DS_Store
Thumbs.db

# Command center generated assets and local runtime data
apps/command-center-prototype/exports/
apps/command-center-prototype/test-artifacts/
apps/command-center-prototype/data/customer-assets/
apps/command-center-prototype/data/*.backup*.json
apps/command-center-prototype/data/*.empty-before-restore*.json

# Runtime data lives outside git (back up separately, see tools/backup-data.mjs)
apps/command-center-prototype/data/command-center.json

# Manual local backups of source files (use git branches instead)
*.local-bad-backup*.js
*backup-2026*.js
*backup-2026*.html
*backup-2026*.css

# Independent git clones vendored for reference only
external/cheat-on-content/
external/juju-content-illustrations/

# Operational one-off scripts — contain LIVE platform cookies and DB credentials.
# Keep local only, NEVER commit (repo is public). Sanitize before versioning any of these.
/scripts/

# Vendored third-party skill (last30days) — not integrated per architecture pivot,
# runtime-only, includes binary assets + cookie-extraction tooling.
apps/command-center-prototype/.agents/

# Python bytecode cache
__pycache__/
*.pyc

# Temp screenshots and scratch files
apps/command-center-prototype/temp_screenshot*.png
tieba_sync2.txt
.codex-import/
external/pixelle-video/
